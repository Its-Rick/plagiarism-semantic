# EduCheck — Academic Check Pipeline
# Install with: pip install -r requirements.txt

# Core API framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Async HTTP (multi-source retrieval)
aiohttp>=3.9.0

# BERT embeddings
sentence-transformers>=2.6.0
torch>=2.0.0

# Numerics
numpy>=1.26.0
scikit-learn>=1.4.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0

# Auth
python-jose[cryptography]>=3.3.0
supabase>=2.0.0

# Scheduling
apscheduler>=3.10.0

# File parsing (frontend upload support)
python-multipart>=0.0.9

# Optional: BM25 retrieval (Stage 2 candidate pre-filtering)
rank-bm25>=0.2.2

# Optional: FAISS for faster ANN search at scale
# faiss-cpu>=1.8.0        # CPU-only (pip install faiss-cpu)
# faiss-gpu>=1.8.0        # GPU variant

# Dev / testing
httpx>=0.27.0             # async test client
pytest>=8.0.0
pytest-asyncio>=0.23.0